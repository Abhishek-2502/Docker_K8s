FLOW OF LEARNING--
1. namespace.yml
2. pod.yml
3. deployment.yml
4. replicaset.yml
5. daemonsets.yml
6. Jobs
    - job.yml
    - Cronjob.yml (Cron is following a particular schedule. (Cron Guru))
7. Volume
    - persistentVolume.yml
    - persistentVolumeClaim.yml
    - deployment.yml
8. service.yml

## Deployment/ReplicaSet/DaemonSet/StatefulSet

We can do rolling update on Deployment but not on ReplicaSet.

DaemonSet ensure every node should have atleast 1 running ReplicaSet.


